# Use plus for string addition

JS-like Chen will use `+` for string concatenation and keep convenient string conversion for mixed string additions. This matches the old Chen surface and JavaScript user expectations better than introducing a separate concatenation operator.
