# Start with prototype-first object model

JS-like Chen will start its JavaScript-style object model with prototype lookup and `this` binding, leaving class syntax out of the first stage. This captures the core object behavior without adding class syntax before the underlying model is settled.
