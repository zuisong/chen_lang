# Use chen.js source extension

JS-like Chen source files will use the `*.chen.js` extension. This gives IDEs a strong JavaScript-like signal while making it clear that the file is Chen source rather than ordinary JavaScript; `.cjs` is avoided because it already means CommonJS JavaScript.
