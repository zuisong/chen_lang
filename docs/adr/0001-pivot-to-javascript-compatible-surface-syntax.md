# Pivot to JavaScript-compatible surface syntax

Chen Lang will redirect its syntax direction toward JavaScript-compatible surface syntax with JavaScript method semantics, while keeping Chen-specific capabilities explicit under a `Chen` extension namespace. This keeps the language visually close to JavaScript without claiming full ECMAScript or JavaScript runtime compatibility.
