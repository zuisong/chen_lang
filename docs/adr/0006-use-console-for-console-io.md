# Use console for console I/O

JS-like Chen will use a JavaScript-familiar `console` runtime object for console input and output instead of the old `stdlib/io` module. This makes basic output look natural to JavaScript users and easier for IDEs to understand through familiar names such as `console.log`.
