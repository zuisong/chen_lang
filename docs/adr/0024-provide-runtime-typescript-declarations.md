# Provide runtime TypeScript declarations

JS-like Chen will provide TypeScript `.d.ts` declarations for runtime globals such as `Chen`, `console`, `JSON`, and `Object`. The declarations are needed because IDE friendliness is a migration goal even though Chen is not a JavaScript engine.
