# Use JSON global object

JS-like Chen will use the JavaScript-familiar `JSON` global object for serialization and parsing instead of the old `stdlib/json` module or a `Chen.json` namespace. This improves IDE friendliness and aligns with JavaScript user expectations.
