# Use JavaScript-style exception syntax

JS-like Chen will use JavaScript-style exception syntax with `try`, `catch (error)`, `finally`, and `throw`. This keeps Chen's existing exception capability while making the surface syntax familiar to JavaScript users.
