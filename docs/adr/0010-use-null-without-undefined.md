# Use null without undefined

JS-like Chen will keep `null` as the only empty-value concept and will not introduce JavaScript `undefined`. This avoids the complexity of JavaScript's two empty values while still allowing source code to look JavaScript-like.
