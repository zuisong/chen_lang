# Use JavaScript collection APIs

JS-like Chen will use JavaScript-familiar collection APIs, including `.length` for arrays and strings and `Object.keys(obj)` or `Object.entries(obj)` for object enumeration. This replaces old colon-style collection methods such as `:len()`, `:keys()`, and `:entries()` for the normal language surface.
