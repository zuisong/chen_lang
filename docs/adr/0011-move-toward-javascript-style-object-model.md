# Move toward JavaScript-style object model

JS-like Chen will move object behavior toward a JavaScript-style object model rather than a Lua-style metatable model. Chen-specific object hooks may remain available through the `Chen` namespace, but the normal object experience should feel like JavaScript.
