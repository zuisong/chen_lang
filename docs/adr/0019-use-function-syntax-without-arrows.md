# Use function syntax without arrows

JS-like Chen will use JavaScript-style `function` declarations and `function` expressions instead of old `def` syntax. Arrow functions are out of scope for the first stage because they introduce separate lexical `this` semantics before the method model is settled.
