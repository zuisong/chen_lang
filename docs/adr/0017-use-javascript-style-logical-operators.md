# Use JavaScript-style logical operators

JS-like Chen will use JavaScript-style logical operators: `&&` and `||` return one of their operands according to Chen-JS truthiness, while `!` returns a boolean. This keeps expression behavior aligned with JavaScript-like source syntax.
