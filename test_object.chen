let obj = #{ name: "Chen", age: 25 }
println(obj.name)
println(obj.age)

obj.city = "Shanghai"
println(obj.city)

let key = "name"
println(obj[key])

obj["country"] = "China"
println(obj["country"])

let person = #{
    name: "Alice",
    address: #{
        city: "Beijing",
        zip: 100000
    }
}
println(person.name)
println(person.address.city)
